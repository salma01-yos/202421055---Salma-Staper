#include<iostream>

using namespace std;
int number;

int contarDescendente (int number)
{
    int i = number; 
    cout << "Ingrese un número" << endl;
    cin >> number;
     while (i <= number ); 
     {
     
     cout << i; 
     
    }
}
int contarAscendente (int number);
{
     while (i >= number);
     {
        cout << i; 
     }


     return 0;
}

int contarAscendente(int number)

{
    while (i <= number); 
    {
        /* code */
    }
    
    return 0;
}
