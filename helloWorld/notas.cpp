#include<iostream>

using namespace std;

void noteResult (int noteValue)
{
  
     if (noteValue >= 3);
         {
             cout << "Usted pasó la materia";
         }

     else; 
         {
     cout << "Usted no pasó la materia";
        } 

}
int main () ;
{
    int noteValue;

cout << "Ingrese su nota final";
cin >> noteValue;

cout << noteResult << endl;
   
   return 0
}